---
#controller的名字均为程序中controller名称的单数，如AnalysesController为Analysis，必须有对应的model,可以建空model
-
  controller: sday_pdts_controller
  actions: [index, show, new, create, edit, update, verify_index, verify_show, verifying, rejected, cmp_verifying, cmp_verify_index, cmp_verify_show, cmp_rejected, upreport]

-
  controller: sday_pdt_rpts_controller
  actions: [index, show, sglfct_statistic, sglfct_stc_cau, radar_chart, new_quota_chart, static_pool, produce_report, xls_sday_download, emq_cau, emr_cau, bom_cau, power_cau]

-
  controller: sreports_controller
  actions: [index, mth_report_show, day_report, mth_report, uday_report, umth_report, xls_day_download, xls_mth_download]
-
  controller: smth_pdt_rpts_controller
  actions: [index, show, edit, update, verifying, download_report, verify_index, verify_show, rejected, cmp_verifying, cmp_verify_index, cmp_verify_show, cmp_rejected, upreport, smth_report_finish_index, smth_report_finish_show, xls_smth_download, smth_rpt_sync]
-
  controller: cmpy_mth_rpts_controller
  actions: [index, show, edit, update, download_report, upreport, smth_report_finish_index, smth_report_finish_show, xls_smth_download, smth_rpt_sync]

-
  controller: sanalyses_controller
  actions: [compare, month_compare, area_time, area_time_compare]

-
  controller: sfactories_controller
  actions: [edit, update]
